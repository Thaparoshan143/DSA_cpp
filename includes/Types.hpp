#pragma once

#include<iostream>
#include<string>
#include<vector>


using String = std::string;
template<class T>
using Vec = std::vector<T>;

using uint = unsigned int;
using uchar = unsigned char;
